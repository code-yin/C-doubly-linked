# C Doubly Linked List

A simple implementation of a doubly linked list in C.

File Structure：
dlist.h — Header file with function declarations and node structure
dlist.c — Implementation of all doubly linked list functions
main.c — Example usage and test cases


Compile & Run：
```bash
gcc main.c dlist.c -o dlist
./dlist
